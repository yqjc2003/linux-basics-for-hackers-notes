# Linux Basics for Hackers
## 模块 0 - 入门与环境搭建

> 本文档为 Module_00_Getting_Started.md 的简体中文翻译版本

---

## 概述

在开始之前,你需要一个安全的地方来练习。你不会希望在真正学习的过程中损坏自己的电脑。本模块的目标就是搭建这样一个安全空间——一台运行 Kali Linux 的虚拟机(VM),并理解 Linux 为何是黑客首选的操作系统。

---

## 为什么选择 Linux?

Windows 对你隐藏了大量信息。大多数事情都在后台进行,你本不该关心它们。Linux 则恰恰相反——一切都是可见的、一切都是可配置的,没有任何东西被锁起来。

Linux 在黑客领域有两点独特之处:

- **开源(Open Source)** - 整个源代码都是公开的。你可以阅读、修改、重新编译,没有任何黑箱。
- **透明(Transparent)** - 你可以随时清楚地看到系统正在做什么。不存在无法查看的隐藏进程。

可以这样理解:Windows 像一辆引擎盖被焊死的车。你可以正常驾驶,但你无法触碰引擎。Linux 则是一辆引擎盖始终敞开、每件工具都摆在桌面上的同款车。

---

## 什么是 Kali Linux?

Linux 有许多版本,称为**发行版(Distribution,简称 distro)**。Ubuntu、Fedora、Mint 都是 Linux,只是针对不同目的以不同方式打包。

Kali Linux 是一个专为安全从业者打造的发行版。它预装了几百种黑客与渗透测试工具,无需你逐个寻找安装。它构建于 **Debian** 之上,而 Debian 是最古老、最稳定的 Linux 发行版之一。

Kali 是业界标准。专业人士使用的就是它,所以我们也使用它。

---

## 虚拟机

虚拟机(Virtual Machine, VM)就是运行在你电脑里的一台"电脑"。它从你的硬件中切出一部分——一些内存、一些 CPU——用来在自己的窗口中运行一套完全独立的操作系统。

```
你的真实电脑(Windows 或 macOS)
└── VirtualBox
    └── Kali Linux VM  ← 这就是你练习的地方
```

你的真实电脑称为**宿主机(Host)**。虚拟机称为**客户机(Guest)**。客户机内部发生的任何事情都只发生在客户机内部。如果你搞坏了什么,重置虚拟机就一切恢复,你的宿主机甚至根本不会察觉到。

这就是为什么专业人士总是先在虚拟机里测试漏洞利用(Exploit)。如果不小心运行了破坏性命令,受损的是虚拟机,而不是你的真实系统。

---

## VirtualBox 与 ISO 文件

**VirtualBox** 是用于创建和运行虚拟机的免费软件。它由 Oracle 开发,可在 Windows、macOS 和 Linux 上运行。

**ISO 文件** 是一种光盘镜像——将一整套操作系统打包成一个文件。当你下载 Kali Linux 时,你下载的就是一个 ISO。VirtualBox 使用该 ISO 在你的虚拟机中安装 Kali,就像从实体光盘安装操作系统一样。

---

## 安装前的检查事项

**必须在 BIOS 中开启虚拟化支持。** 这是 VirtualBox 启动虚拟机失败的常见原因。你的 CPU 支持虚拟化,但有时它在 BIOS 设置中默认是关闭的。

- Intel CPU 称之为:**Intel VT-x**
- AMD CPU 称之为:**AMD-V**

查看方法:重启电脑并按 `DEL`、`F2` 或 `F12`(取决于主板型号)进入 BIOS。找到虚拟化选项并确保已开启。

**内存分配。** 不要给虚拟机分配过多内存,否则宿主机将严重变慢。简单原则——电脑每有 4 GB 内存,就分给虚拟机 1 GB。

| 你的总内存 | 分给虚拟机 |
|---|---|
| 4 GB | 1 GB |
| 8 GB | 2 GB |
| 16 GB | 4 GB |

**root 密码。** 安装过程中,Kali 会要求你设置 root 密码。Root 是拥有最高权力的管理员账户——对系统中的一切拥有完全控制权。设置一个你能记住的密码,即便简单也无妨,之后可以再修改。

---

## 安装步骤

1. 从 virtualbox.org 下载并安装 VirtualBox。
2. 从 kali.org/get-kali 下载 Kali Linux 64 位安装版 ISO。
3. 打开 VirtualBox → 点击"新建" → 命名为 "Kali" → 类型: Linux → 版本: Debian 64-bit。
4. 按照上表分配内存。
5. 创建虚拟硬盘,至少 20 GB。
6. 进入 设置 → 存储 → 点击空白的光盘图标 → 挂载你的 ISO 文件。
7. 启动虚拟机并按 Kali 安装程序指引完成安装。
8. 按提示设置用户名与 root 密码。

当看到带有龙形图标的 Kali 登录界面时,就说明安装成功了。

---

## 关键术语参考

| 术语 | 含义 |
|---|---|
| 开源(Open Source) | 代码是公开的——任何人都可以阅读或修改 |
| 发行版(Distribution / distro) | 为特定用途打包的 Linux 版本 |
| Debian | Kali 所基于的稳定 Linux 底层 |
| 虚拟机(VM) | 运行在你真实电脑里的"假电脑" |
| VirtualBox | 用于创建和运行虚拟机的软件 |
| ISO 文件 | 用于安装操作系统的可下载磁盘镜像 |
| 宿主机(Host) | 你真实的、物理的电脑 |
| 客户机(Guest) | 运行在宿主机内的虚拟机 |
| Root | Linux 中拥有最高权力的管理员用户 |

---

## 练习

- [ ] 下载 VirtualBox 和 Kali 64 位 ISO
- [ ] 创建一个名为 "Kali" 的虚拟机并成功运行
- [ ] 进入带有龙形壁纸的登录界面——这既是本模块的终点

> 提示:*如需更深入的练习,建议同步完成官方 **Linux Basics for Hackers** 一书中各章末尾的练习题。*
---

*下一章:模块 1 - 终端基础*