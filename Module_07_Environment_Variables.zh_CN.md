# Linux 黑客基础
## 第 7 模块 - 环境变量

> 本文档为 Module_07_Environment_Variables.md 的简体中文翻译版本

---

## 概述

你的 Linux 系统运行依赖一组幕后的设置,这些设置控制着一切的行为——在哪里查找程序、你是谁、你的主目录在哪、终端提示符长什么样。这些被称为**环境变量(Environment Variables)**。它们始终存在,你只是还没关注过它们。

---

## 环境变量到底是什么

环境变量只是系统在内存中为你登录期间保存的一个带名称的值。程序和 Shell 本身会不断读取这些值来决定如何表现。

有些在系统启动时由系统设置。有些在你登录时设置。而你可以随时创建或修改自己的变量。

命名约定很简单:变量名通常全大写,你通过在名称前加 `$` 来引用它们。

```bash
ahegazy0@kali:~$ echo $HOME
/home/kali
```

---

## 重要的变量

| 变量 | 含义 |
|---|---|
| `$PATH` | 当你输入命令时,系统搜索的文件夹列表 |
| `$HOME` | 你的主目录路径 |
| `$USER` | 你当前的用户名 |
| `$SHELL` | 你正在运行的 Shell(通常是 /bin/bash) |
| `$HISTSIZE` | 你的历史文件保留多少条命令 |
| `$PS1` | 你的命令提示符的样子 |

---

## PATH - 最重要的一个

`$PATH` 是一个用冒号分隔的文件夹路径列表。当你输入像 `ls` 这样的命令时,系统并不会神奇地知道 `ls` 存放在哪里。它会依次遍历 `$PATH` 中列出的每个文件夹,直到找到同名的程序。

```bash
ahegazy0@kali:~$ echo $PATH
/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
```

每个用 `:` 分隔的文件夹都是系统会搜索的位置。

这就是为什么有时你会遇到 "command not found"(未找到命令)。你安装了一个工具,但它的文件夹不在你的 PATH 中,所以系统不知道去哪里找它。修复方法是将该文件夹添加到 PATH 中,详见下文。

**如果你不小心破坏了 PATH 变量**,你将几乎无法运行任何命令,因为系统找不到它们。这可以修复,但很烦人。编辑时务必小心。

---

## 查看所有环境变量

```bash
ahegazy0@kali:~$ env
```

这会打印当前会话中设置的每一个环境变量。数量会比你预期的多。滚动浏览一遍,你大多数都会认出来。

---

## 修改和创建变量

要为当前终端会话创建或修改变量:

```bash
ahegazy0@kali:~$ MYVAR="hello"
ahegazy0@kali:~$ echo $MYVAR
hello
```

问题在于这会在关闭终端的瞬间消失。要使其对任何子进程(从终端启动的程序)可用,你需要**导出(export)**它:

```bash
ahegazy0@kali:~$ export MYVAR="hello"
```

现在,从该终端启动的任何程序都可以读取 `$MYVAR`。

要将文件夹添加到 PATH 而不覆盖现有的:

```bash
ahegazy0@kali:~$ export PATH=$PATH:/new/folder/here
```

开头的 `$PATH` 保留了之前所有的内容。你只是在末尾追加了 `:new/folder/here`。如果你忘记这部分,直接写 `/new/folder/here`,就会清除其他所有内容,完全破坏 PATH。

---

## 让修改永久生效

`export` 只在当前会话中有效。要使变量在重启和新终端中仍然存在,你需要将其添加到 Shell 的配置文件中。

对于 bash,该文件是 `~/.bashrc`。打开它,在末尾添加你的 export 行:

```bash
ahegazy0@kali:~$ export PATH=$PATH:/your/new/tool/folder
```

保存文件。然后打开新终端,或运行:

```bash
ahegazy0@kali:~$ source ~/.bashrc
```

这会在不重启的情况下重新加载该文件。

---

## 修改提示符 - PS1

`$PS1` 控制命令提示符的外观。默认情况下,它显示类似 `kali@kali:~$` 的内容。你可以将其更改为任何内容。

```bash
ahegazy0@kali:~$ export PS1="Hacker-Level-99: # "
```

你的提示符现在看起来像:

```
Hacker-Level-99: # 
```

这主要是外观上的,但有些人会把提示符设置为显示有用信息——当前目录、所在 Git 分支、时间。现在只需知道 PS1 控制着它,而你可以修改它。

---

## 黑客使用的历史记录技巧

`$HISTSIZE` 控制有多少条命令被保存到历史文件中。当你按上方向键滚动查看过去的命令时,那便是历史。

```bash
ahegazy0@kali:~$ export HISTSIZE=0
```

将其设置为 0 表示不保存任何内容。终端在该会话的剩余时间内停止记录。关闭终端后,任何打开历史文件的人都看不到你运行过的命令。

这是一项基础的操作安全措施。如果你处在一个你不该进入的系统中,并且不想留下痕迹,这是你要做的第一件事之一。

---

## 命令参考

| 命令 | 作用 |
|---|---|
| `env` | 打印所有当前环境变量 |
| `echo $VAR` | 打印指定变量的值 |
| `MYVAR="value"` | 为当前会话创建变量 |
| `export MYVAR="value"` | 创建对子进程也可用的变量 |
| `export PATH=$PATH:/folder` | 向 PATH 添加文件夹而不破坏原有内容 |
| `source ~/.bashrc` | 不重启重新加载你的 bash 配置文件 |

---

## 练习

- [ ] 运行 `env` 并找到你的 `$SHELL` 变量——它会告诉你正在运行的确切的 Shell
- [ ] 运行 `echo $PATH` 并查看列出的每个文件夹——这些都是系统查找程序的位置
- [ ] 尝试 `export HISTSIZE=0`,然后按上方向键——该会话的历史就没了
- [ ] 修改你的 PS1 让它只显示你的名字或自定义内容,观察提示符立即变化

> 💡 *想要更深入的练习,我还建议完成官方 **Linux Basics for Hackers** 一书中各章节末尾的练习题。*
---

*下一篇:第 8 模块 - Bash 脚本编写*