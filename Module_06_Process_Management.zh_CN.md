# Linux 黑客基础
## 第 6 模块 - 进程管理

> 本文档为 Module_06_Process_Management.md 的简体中文翻译版本

---

## 概述

在任意时刻,你的计算机同时运行着数百个进程——你的终端、后台服务、系统守护进程(Daemon,系统服务进程),甚至你自己都没意识到启动的东西。本模块介绍如何查看所有这些进程,理解它们是什么,以及如何对其进行控制。这意味着加快运行速度、迁移任务,以及杀死那些已冻结、消耗资源或仅仅挡路的进程。

---

## 什么是进程?

每次你运行一个程序,系统就会为其创建一个**进程(Process)**。进程就是一个正在运行的程序实例。打开 Firefox——这就是一个进程。运行一条终端命令——这也是一个进程。即使是没有可见窗口、在后台静默运行的东西,也是进程。

需要理解的关键点是,每个进程在启动的瞬间都会被分配一个唯一的编号。这个编号称为 **PID - 进程标识号(Process ID)**。系统(以及你)通过它来识别并引用一个特定的运行任务。如果你想停止某个东西,你需要它的 PID。

两种值得了解的进程类型:

- **前台进程(Foreground processes)** - 可见地运行,附加到你的终端。如果关闭终端,它们就会终止。
- **后台进程(Background processes)** - 在后台静默运行。终端保持空闲,它们继续运行。

![Linux 前台与后台进程](assets/linux_processes_diagram_1789213627295.jpg)

---

## 查看正在运行的进程

### ps

`ps` 为你提供当前正在运行的进程的快照。单独使用时,它只显示与当前终端会话绑定的进程,这并不是很实用。你实际需要使用的版本是:

```bash
ahegazy0@kali:~$ ps aux
```

标志解析:
- `a` - 显示所有用户的进程,而不只是你自己的
- `u` - 显示每个进程的所有者用户
- `x` - 包含不附加于任何终端的进程(后台守护进程)

输出看起来像这样:

```
USER       PID  %CPU  %MEM    VSZ   RSS  STAT  COMMAND
root         1   0.0   0.1  22548  1024  Ss    /sbin/init
kali      1023   0.3   1.2  54320  6200  S     bash
kali      1587   2.1   4.5 412300 22800  Sl    firefox
```

最重要的几列:

| 列名 | 含义 |
|---|---|
| USER | 谁拥有该进程 |
| PID | 进程 ID——这是你用来杀死它的标识 |
| %CPU | 它正在使用多少 CPU |
| %MEM | 它正在使用多少内存 |
| COMMAND | 实际运行的程序是什么 |

`ps aux` 是一个静态快照。它只在运行命令的瞬间显示状态,然后停止更新。

---

### top

`top` 是实时版本。它每隔几秒刷新一次,显示当前正在运行的进程,默认按 CPU 使用率排序——所以消耗资源最多的进程会排在最上面。

```bash
ahegazy0@kali:~$ top
```

在 top 内的常用键:

| 按键 | 作用 |
|---|---|
| `k` | 杀死进程——会询问 PID |
| `M` | 改为按内存使用率排序(而非 CPU) |
| `P` | 按 CPU 使用率排序(默认) |
| `q` | 退出 |

如果你的电脑突然变慢又找不到原因,立即打开 `top`。那个贪资源的进程会位于列表的最顶端。

---

## 在后台运行进程

默认情况下,当你运行一条命令时,它会在你的终端中独占,你无法在它运行时输入其他内容。如果你要启动一个像浏览器或长时间运行的工具之类的东西,并希望取回终端,在命令末尾加上 `&` 即可。

```bash
ahegazy0@kali:~$ firefox &
[1] 2341
```

`[1]` 是任务编号。`2341` 是 PID。Firefox 现在在后台运行,你的终端可以继续使用。

---

## 在前台与后台之间移动进程

一旦某个进程在运行,你就可以在前后台之间移动它。

**暂停一个前台进程**(暂停但并不杀死):

```bash
ahegazy0@kali:~$ Ctrl + Z
```

**将暂停的进程发送到后台**(保持运行,只是不在前台显示):

```bash
ahegazy0@kali:~$ bg
```

**将后台进程移回前台:**

```bash
ahegazy0@kali:~$ fg
```

如果你有多个后台任务在运行,`fg` 会带回最近的一个。要带回特定的任务,使用其任务编号:

```bash
ahegazy0@kali:~$ fg 2
```

查看所有后台任务及其编号:

```bash
ahegazy0@kali:~$ jobs
```

---

## 杀死进程

当你需要停止某个进程时,使用 `kill` 后跟 PID。

```bash
ahegazy0@kali:~$ kill 2341
```

这会发送一个礼貌的终止信号——它请求进程自行干净地关闭。大多数情况下这都能正常工作。

如果进程已冻结或拒绝停止,可以强制终止:

```bash
ahegazy0@kali:~$ kill -9 2341
```

`-9` 标志发送 **SIGKILL** 信号,进程无法忽略、捕获或延迟。它会被立即终止,毫无商量余地。可以把普通的 `kill` 看作礼貌地请某人离开,而 `kill -9` 是强行将其带走。

**小心 root 拥有的进程。** 如果你在不了解其作用的情况下杀死了 root 拥有的系统进程,你可能会破坏系统稳定性或导致系统崩溃。在你不确定的进程上执行 kill 之前,请始终在 `ps aux` 中检查 COMMAND 列。

---

## 在不使用 ps 的情况下查找 PID

如果你已经知道进程的名称,就无需滚动浏览 `ps aux` 的输出。使用 `pgrep`:

```bash
ahegazy0@kali:~$ pgrep firefox
```

直接返回 PID。简洁快速。

或使用 `pidof`:

```bash
ahegazy0@kali:~$ pidof firefox
```

作用相同。个人偏好哪个就用哪个。

---

## 为什么这对黑客很重要

当你进入一个系统时,你要做的第一件事之一就是运行 `ps aux` 并查看正在运行的内容。你在寻找:
- **杀毒软件或终端防护** - 如果你即将运行漏洞利用程序或投递文件,你需要知道谁在监视。找到进程并将其杀死(如果你有权限)是一个常见步骤。
- **有趣的服务** - 数据库、Web 服务器、内部工具。这些可以告诉你这台机器是做什么用的,以及可能值得攻击的目标。
- **其他用户的进程** - 如果有多个用户登录,他们的进程也会出现在这里。这些都是有用的信息。

`ps aux` 既是一个管理工具,也是一个侦察工具。

---

## 命令参考

| 命令 | 作用 |
|---|---|
| `ps aux` | 所有运行进程的快照 |
| `top` | 进程实时视图,按资源使用排序 |
| `kill PID` | 礼貌地请求进程停止 |
| `kill -9 PID` | 立即强制停止进程 |
| `pgrep name` | 按名称获取进程的 PID |
| `pidof name` | 同 pgrep,语法稍有不同 |
| `jobs` | 列出当前会话的所有后台任务 |
| `fg` | 将最近的后台任务带到前台 |
| `bg` | 将暂停的任务发送到后台 |
| `command &` | 从一开始就以后台方式启动命令 |
| `Ctrl + Z` | 暂停正在运行的前台进程 |
| `updatedb` | (来自上一模块)仅作提示——需要 root |

---

## 关于信号的说明

`kill` 不仅仅是"杀死"——它向进程发送**信号(Signal)**。`-9`(SIGKILL)是最激进的一个。但还有一些其他信号最终值得了解:

| 信号 | 编号 | 作用 |
|---|---|---|
| SIGTERM | 15 | 礼貌的关闭请求(默认) |
| SIGKILL | 9 | 立即强制终止 |
| SIGHUP | 1 | 不重启情况下重新加载配置 |

你大部分时间只会用到 9 和 15。其他信号在你处理服务和守护进程时会再遇到。

---

## 练习

- [ ] 运行 `ps aux` 并找到你的终端进程——记下它的 PID
- [ ] 打开 `top` 并观察其更新。找出当前占用 CPU 最多的进程
- [ ] 在后台运行 `leafpad &`(或任何文本编辑器),然后用 `pgrep` 找到它的 PID,再用 `kill -9` 杀死它
- [ ] 尝试在运行中的命令上使用 `Ctrl + Z`,然后用 `fg` 将其恢复

要熟悉的是"杀死后台进程"的整套流程。运行某个东西,找到它的 PID,杀死它。多做几次,直到感觉自动化。

> 💡 *想要更深入的练习,我还建议完成官方 **Linux Basics for Hackers** 一书中各章节末尾的练习题。*
---

*下一篇:第 7 模块 - 管理用户环境变量*