# Linux Basics for Hackers
## Module 13 - 安全与匿名(Security & Anonymity)

> 本文档为 Module_13_Security_Anonymity.md 的简体中文翻译版本

---

## 概述(Overview)

你在网上做的每件事都会留下痕迹。你访问的每个网站、你的机器建立的每个连接 —— 你的真实 IP 地址都与之绑定。本模块就是介绍这种跟踪是如何工作的,以及 Tor、proxy、VPN 和 proxychains 等工具如何减少你的暴露。这些内容既对保护自己有用,也对理解隐私意识强的人(以及攻击者)所依赖的基础设施有帮助。

---

## 你的 IP 地址为什么重要

你的 IP 地址由你的 ISP(互联网服务提供商)分配。你与互联网上任何服务器建立的每一个连接都携带这个 IP。你连接的服务器可以看到它。你的 ISP 可以看到你连接的所有内容。你所在的任何网络都能看到你的流量。

这正是匿名工具要解决的问题 —— 它们在你和目的地之间放一层中间环节,这样目的地看到的是代理或中继的 IP,而不是你的真实 IP。

---

## 代理(Proxy)

代理(proxy)是介于你和目的地之间的服务器。你不是直接连接到某个网站,而是先连接到代理,然后由代理代替你连接到该网站。网站看到的是代理的 IP,而不是你的。

```
你 → 代理 → 网站
```

网站日志记录的是代理的 IP 地址。你的 ISP 只能看到你连接到了代理,但看不到你在代理上做了什么。

单个代理是最弱的匿名形式。代理服务器本身知道你的真实 IP,也知道你要去哪里。如果有人传唤该代理,或者它保留了日志,你的活动就可追溯。

**免费代理几乎总是糟糕的选择**,特别是对于任何敏感的事情。许多免费代理专门被设置用来抓取流量 —— 设计为记录所有流经数据的蜜罐(honeypot)。如果你不放心让一个随机陌生人帮你送信,也不要信任随机的免费代理。

---

## Tor —— 洋葱路由器

Tor 通过三个服务器组成的链(称为 **节点(node)** 或 **中继(relay)**)来转发你的流量,这三台服务器由世界各地的志愿者运行。每个中继只知道它前一步和后一步的信息 —— 没有任何一个中继同时知道你是谁和你要去哪里。

```
你 → 入口节点(Entry Node) → 中间节点(Middle Node) → 出口节点(Exit Node) → 网站
```

加密方式是分层的 —— 像一个洋葱一样。你的流量被包裹在三层加密中。每个节点剥掉一层以知道下一步往哪里发送,但无法读取真实内容,也无法看到完整路径。

网站看到的是出口节点的 IP,不是你的。入口节点知道你的 IP,但不知道目的地。中间节点两者都不知道。

**在 Kali 里怎么用:**

```bash
ahegazy0@kali:~$ apt install tor
ahegazy0@kali:~$ service tor start
```

或者下载 Tor 浏览器,它是一个预先配置好的 Firefox,自动把所有流量都走 Tor。

**权衡:** Tor 很慢。经过分布在世界各地的三个中继节点会带来明显的延迟。它不适合高带宽活动或任何对实时性要求高的场景。它也不是魔法盾牌 —— 应用层的糟糕安全操作(比如登录了你的个人账号)可以彻底击败它。

---

## Proxychains —— 强制工具走代理

Tor 和 VPN 保护你的浏览器流量。但如果你在运行一个终端工具 —— 比如 `nmap`、`curl` 或自定义脚本 —— 它的流量会直接发出去,除非你通过某种方式对它进行路由。

`proxychains` 是一个工具,可以拦截任何程序的网络调用,并强制它们通过你配置好的一条代理链。你在任意命令前面加上它,流量就会被重定向。

```bash
ahegazy0@kali:~$ proxychains nmap -sT 192.168.1.1
```

现在 nmap 的流量会先经过你的代理链,再到达目标。

**配置文件:**

```bash
ahegazy0@kali:~$ nano /etc/proxychains.conf
```

文件底部你会看到代理列表。按以下格式添加代理:

```
socks5  127.0.0.1  9050    ← 这是 Tor 的本地端口
socks4  10.0.0.1   1080
http    203.0.113.5  3128
```

如果你加上 `127.0.0.1 9050`(Tor 的默认本地端口),并且 Tor 正在运行,proxychains 就会把所有流量都通过 Tor 转发。结合使用,这意味着你的终端工具也会走 Tor 网络。

**配置文件中值得了解的两个模式:**

- `strict_chain` —— 流量必须按顺序经过每一个代理;如果某个不可用,连接就会失败
- `dynamic_chain` —— 自动跳过不可用的代理,使用任何可用的代理

对大多数使用场景来说,`dynamic_chain` 更实用。

---

## VPN

VPN(Virtual Private Network,虚拟专用网络)在你和 VPN 服务器之间建立一个加密隧道。你的所有流量都通过这条加密隧道传输,然后再进入互联网。你的 ISP 只能看到你连接到了一个 VPN,看不到你在做什么。

```
你 → [加密隧道] → VPN 服务器 → 互联网
```

与 Tor 相比:
- VPN 更快
- 只涉及一台服务器 —— VPN 提供商如果愿意可以记录你的流量和日志
- VPN 提供商知道你的真实 IP

口碑好、有经过审计的无日志政策的付费 VPN 提供商是标准选择。关键短语是"无日志政策(no-log policy)" —— 你需要的是一个能够证明确实不保留你活动记录的服务商。

**VPN + Tor 组合** 是一些人为获得最大层级匿名所使用的方案:

```
你 → VPN → Tor 网络 → 网站
```

你的 ISP 看到的是 VPN 流量。VPN 提供商看到你连到了 Tor,但看不到目的地。Tor 出口节点完全不知道你是谁。网站看到的是 Tor 的出口节点。

代价是比单独使用 Tor 还要更高的延迟。

---

## 加密通信

匿名化网络流量是一方面。你实际通信的内容也很重要。

**ProtonMail** 是一家端到端加密的邮件服务提供商。ProtonMail 用户之间的邮件在服务器端是加密的,ProtonMail 自己也无法读取。即使接到法院命令,也没有可上交的内容,因为密钥只存在于用户自己的设备上。

对一般消息来说,**Signal** 遵循同样的原则 —— 默认端到端加密,只保留最少的元数据(关于通信的时间、参与者、位置等信息,而不是通信内容本身)。

这些是记者、活动人士、律师,以及任何处理敏感通信的人所使用的标准工具。使用加密通信本身并不可疑 —— 这是基本的安全卫生习惯。

---

## 真正会破坏匿名的是什么

工具的好坏取决于你怎么使用它们。大多数匿名失败发生在人的层面,而不是技术层面:

- 使用 Tor 时登录个人账号(Google、社交媒体) —— 不管 IP 是什么,账号本身就暴露了你的身份
- 在匿名和非匿名活动中重复使用相同的用户名
- 浏览器指纹 —— 即使没有 cookie,你的浏览器也会泄露有关操作系统、屏幕分辨率、已安装字体、时区等信息
- 文件中的元数据 —— 文档和照片经常包含关于创建它们的设备的内嵌数据
- 把敏感流量托付给免费或来路不明的代理

匿名是一种实践,而不只是一个你装上去的工具。

---

## 命令参考(Command Reference)

| 命令 | 说明 |
|---|---|
| `service tor start` | 启动 Tor 服务 |
| `proxychains [command]` | 通过你的代理链运行任意命令 |
| `nano /etc/proxychains.conf` | 编辑 proxychains 配置 |
| `curl ifconfig.me` | 查看互联网看到你的 IP 是什么 |
| `proxychains curl ifconfig.me` | 通过 proxychains 查看你显现的 IP |

---

## 练习(Practice)

- [ ] 安装并启动 Tor,然后运行 `proxychains curl ifconfig.me` —— 返回的 IP 应该不是你真实的 IP
- [ ] 打开 `/etc/proxychains.conf` 查看配置 —— 把 Tor 的本地端口(`socks5 127.0.0.1 9050`)加到代理列表中
- [ ] 直接访问一个"我的 IP 是什么"的网站,然后通过 Tor 浏览器再访问 —— 比较这两个 IP
- [ ] 如果你还没有 ProtonMail 账户,看看它 —— 它是免费的,任何敏感的事都值得用它

> 💡 *如需进行更深入的练习,我推荐同时完成官方 **Linux Basics for Hackers** 一书中本章末尾的习题。*
---

[下一篇:Module 14 - 无线网络(Wireless Networking)](Module_14_Wireless_Networking.zh_CN.md)
