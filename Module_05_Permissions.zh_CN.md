# Linux 黑客基础
## 第 5 模块 - 权限与特权

> 本文档为 Module_05_Permissions.md 的简体中文翻译版本

---

## 概述

Linux 是一个多用户系统。多个人可以在同一台机器上拥有账户,但并不是每个人都应该能够读取、修改或运行所有内容。权限就是 Linux 强制实施这一规则的方式。本模块涵盖如何查看权限、如何修改权限,以及理解权限在哪里会成为安全问题。

---

## 权限如何工作

Linux 中的每个文件和文件夹都附加了三个权限组:

- **所有者(Owner)** - 创建文件的用户
- **用户组(Group)** - 一组共享访问权限的用户
- **其他人(Others)** - 系统上的其他所有人

这三组中的每一组都可以拥有三种类型的权限:

| 权限 | 符号 | 含义 |
|---|---|---|
| 读取(Read) | `r` | 可以查看文件内容 |
| 写入(Write) | `w` | 可以修改或删除文件 |
| 执行(Execute) | `x` | 可以将文件作为程序运行 |

![Linux 文件权限](assets/file_permissions_diagram_1789213222578.jpg)

当你运行 `ls -l` 时,第一列会显示每个文件的权限:

```bash
ahegazy0@kali:~$ ls -l
-rwxr-xr--  1  kali  kali  4096  Jan 1  file.sh
```

开头的这一串字符就是权限块。分解开来:

```
- rwx r-x r--
│  │   │   │
│  │   │   └── 其他用户(Others):  仅可读
│  │   └────── 用户组(Group):   可读 + 可执行
│  └────────── 所有者(Owner):   可读 + 可写 + 可执行
└───────────── 文件类型: - 表示普通文件,d 表示目录
```

如果你下载了一个黑客工具但它拒绝运行,通常就是这个原因——执行位没有设置。

---

## chmod - 修改权限

`chmod` 代表 "change mode"(修改模式)。它用于设置谁可以对文件做什么。

有两种使用方式:数字方式和符号方式。

### 数字方式

每种权限都有一个数值:

| 权限 | 数值 |
|---|---|
| 读取 (r) | 4 |
| 写入 (w) | 2 |
| 执行 (x) | 1 |

将它们相加即可得到该组的权限值。然后写出三位数字——第一位为所有者,第二位为用户组,第三位为其他人。

| 数字 | 计算 | 权限 |
|---|---|---|
| 7 | 4+2+1 | 读取 + 写入 + 执行 |
| 6 | 4+2 | 读取 + 写入 |
| 5 | 4+1 | 读取 + 执行 |
| 4 | 4 | 仅读取 |
| 0 | 0 | 完全没有权限 |

示例:

```bash
ahegazy0@kali:~$ chmod 755 script.sh
```

所有者得到 7(读+写+执行)。用户组得到 5(读+执行)。其他人得到 5(读+执行)。这是希望能够运行但不允许他人修改的脚本的标准权限。

```bash
ahegazy0@kali:~$ chmod 644 notes.txt
```

所有者得到 6(读+写)。用户组得到 4(读)。其他人得到 4(读)。这是他人可读但只能由你编辑的文件的标准权限。

```bash
ahegazy0@kali:~$ chmod 777 file.sh
```

> **注意事项:** `chmod 777` 对于无法运行的脚本来说是"终极偷懒解法",但这意味着系统上的*任何人*都可以修改你的文件。在渗透测试考试或真实环境里,盲目地将敏感文件设为 777 会直接导致安全失败。请习惯使用 `chmod +x`!

### 符号方式

用字母代替数字来指定变更:

```bash
ahegazy0@kali:~$ chmod +x script.sh
```

为所有人添加执行权限。

```bash
ahegazy0@kali:~$ chmod u+x script.sh
```

仅为所有者(`u` = user/owner)添加执行权限。

```bash
ahegazy0@kali:~$ chmod g-w file.txt
```

从用户组移除写入权限。

字母含义: `u` = 所有者,`g` = 用户组,`o` = 其他人,`a` = 以上三者。

---

## chown - 修改所有者

chown 通过 "change owner" 缩写而来。它用于重新分配文件的所有者。

```bash
ahegazy0@kali:~$ chown kali file.txt
```

将所有者更改为 `kali` 用户。

```bash
ahegazy0@kali:~$ chown kali:kali file.txt
```

同时更改所有者和用户组为 `kali`。

```bash
ahegazy0@kali:~$ chown -R kali /home/kali/tools
```

`-R` 标志表示递归(recursive)应用——它会更改文件夹及其内部所有内容的所有者。

修改不属于你的文件的所有者需要 root 权限。

---

## SUID - 重要的一个

SUID 代表 "Set User ID"(设置用户 ID)。它是一个特殊权限位,会改变文件的执行方式。

通常当你运行一个程序时,它会以你的权限运行。如果某个由 root 拥有的文件设置了 SUID 位,那么即使你是普通用户,它也会以 root 的权限运行。

在权限串中,SUID 显示为所有者执行位位置上的 `s`:

```
-rwsr-xr-x   root   root   /usr/bin/passwd
```

所有者字段中的这个 `s` 表示 SUID 已设置。`passwd` 命令需要 SUID,因为任何用户更改密码时,它必须写入 `/etc/shadow`(一个由 root 拥有的文件)。

SUID 在有意设置的情况下是合法的。问题出现在文件被错误地设置了 SUID,或者编写不当的 SUID 程序被欺骗去做不该做的事情时。

**这就是权限提升(Privilege Escalation)的切入点。** 权限提升是指通过利用权限错误,从受限用户账户升级到 root 的过程。找到配置错误的 SUID 二进制文件就是经典的实现方式之一。

要查找系统上所有设置了 SUID 位的文件:

```bash
ahegazy0@kali:~$ find / -perm -u=s -type f 2>/dev/null
```

命令解析:
- `find /` - 从根目录开始搜索
- `-perm -u=s` - 查找设置了 SUID 位的文件
- `-type f` - 仅文件,不包括目录
- `2>/dev/null` - 隐藏权限错误,使输出更干净

在任何 Linux 系统上运行此命令并查看结果。如果某个文件存在已知漏洞或配置错误,它就是一条潜在的提权路径。

---

## 关于权限提升的简短说明

深入理解权限的全部意义在于:系统在用户层面被攻破的情况屡见不鲜。攻击者以低权限用户身份进入,然后寻找获取 root 的方法。这个过程就是权限提升。

权限配置错误是最常见的攻击向量之一——不该设置 SUID 的二进制文件被设置了 SUID,不该被某些用户写入的文件被设置为可写,由 root 拥有但人人可编辑的脚本等。理解权限的工作原理,无论是攻击还是防御,都是发现这些错误的关键。

---

## 命令参考

| 命令 | 作用 | 示例 |
|---|---|---|
| `ls -l` | 显示文件权限 | `ls -l` |
| `chmod 755 file` | 以数字方式设置权限 | `chmod 755 script.sh` |
| `chmod +x file` | 为所有人添加执行权限 | `chmod +x tool.py` |
| `chmod u+x file` | 仅为所有者添加执行权限 | `chmod u+x run.sh` |
| `chown user file` | 更改文件所有者 | `chown kali file.txt` |
| `chown user:group file` | 同时更改所有者和用户组 | `chown kali:kali file` |
| `chown -R user dir` | 递归更改所有者 | `chown -R kali /tools` |
| `find / -perm -u=s -type f 2>/dev/null` | 查找所有 SUID 文件 | - |

---

## 练习

- [ ] 使用 `touch test.txt` 创建一个文件,然后运行 `ls -l` 查看其默认权限
- [ ] 使用 `chmod 600 test.txt`——这会让你拥有唯一的读和写权限,其他人无任何权限。用 `ls -l` 确认
- [ ] 创建一个简单的脚本,尝试运行它,观察失败,然后使用 `chmod +x` 并再次运行
- [ ] 运行上面的 `find` 命令列出系统上所有 SUID 文件——查看返回的结果

> 💡 *想要更深入的练习,我还建议完成官方 **Linux Basics for Hackers** 一书中各章节末尾的练习题。*
---

*下一篇:第 6 模块 - 进程管理*