# Linux Basics for Hackers
## 模块 1 - 终端基础

> 本文档为 Module_01_Terminal_Basics.md 的简体中文翻译版本

---

## 概述

在 Windows 中,你靠点击操作;在 Linux 中,你靠输入命令。这正是本模块要介绍的核心转变。终端将是你作为黑客花费大部分时间的地方,你最终会用到的每一个工具都通过它运行。这一步无法跳过。

---

## 终端的本质

终端是一个文本窗口,你向电脑下达直接指令,电脑作出响应。没有图标、没有菜单、不需要鼠标。只有你输入,系统精确执行你所说的内容。

大多数黑客工具根本没有图形界面——没有按钮、没有窗口、没有可点击的东西。如果你不会使用终端,你就无法使用这些工具。事情就是这么简单。

值得把终端视为与电脑的一种对话。你无需到处寻找文件夹并双击打开,只需输入你想去的位置,瞬间即达。起初它让你觉得迟缓,然而总有一天你会发现它比用过的任何 GUI 都要快。

---

## Linux 文件系统 - 倒置的树

Linux 将一切存储在一个单一的结构中,从顶部的一个起点向下分支。这个起点称为**根(root)**,写为 `/` —— 就是一个正斜杠。

系统中的所有内容——文件、文件夹、程序、设置——都存在于这棵树的某处。

![Linux 文件系统层次结构](assets/linux_filesystem_tree_1789211965428.jpg)

```
/
├── etc/        系统设置与配置文件
├── bin/        系统自带的基础程序
├── home/       每个用户的个人文件夹
│   └── kali/   你的主文件夹
├── root/       root(管理员)用户的主文件夹
├── var/        不断变化的日志与数据
├── tmp/        临时文件,每次重启都会清空
└── usr/        已安装的软件与应用
```

如果你来自 Windows:Windows 用 `C:\Users\YourName\` 作为个人主目录。Linux 则使用 `/home/yourname/`。概念相同,语法不同。

目前你需要记住的重要目录如下:

| 目录 | 内容 |
|---|---|
| `/` | 根目录——一切的最顶层 |
| `/etc` | 配置文件——系统设置都在这里 |
| `/bin` | 系统运行所需的基础程序 |
| `/home` | 普通用户的个人文件夹 |
| `/root` | 专门为 root 用户准备的主文件夹 |
| `/tmp` | 临时文件——每次重启都会清空 |

---

## 必备命令

### pwd

全称 "Print Working Directory"(打印当前工作目录)。告诉你此刻在文件系统中的确切位置。

```bash
ahegazy0@kali:~$ pwd
/home/kali
```

你正位于 `home` 目录下的 `kali` 文件夹,在系统根中。当你迷路时随时使用它。

---

### ls

全称 "List"(列表)。显示当前目录的内容。

```bash
ahegazy0@kali:~$ ls
Desktop  Documents  Downloads  Pictures
```

常用的变体:

| 命令 | 变化之处 |
|---|---|
| `ls` | 基础列表 |
| `ls -l` | 显示权限、大小、日期等详细信息 |
| `ls -a` | 同时显示隐藏文件 |
| `ls -la` | 显示包含隐藏文件在内的详细信息 |

Linux 中的隐藏文件以点开头——例如 `.bashrc` 或 `.config`。普通的 `ls` 不会显示它们。需要查看全部内容时使用 `ls -la`,你会经常用到它。

---

### cd

全称 "Change Directory"(切换目录)。这是你在文件系统中移动的方式。

```bash
ahegazy0@kali:~$ cd /etc
```

直接跳转到 `/etc`。

值得记住的快捷方式:

| 命令 | 跳转位置 |
|---|---|
| `cd /path` | 直接进入所输入的路径 |
| `cd ..` | 上一级(父目录) |
| `cd ~` | 直接进入主文件夹 |
| `cd /` | 直接跳转到根目录 |
| `cd -` | 回到上一次所在的目录 |

`cd ..` 是你最常用的命令。如果你在目录树深处想返回上层,反复输入 `cd ..` 即可到达目标位置。

---

### man

全称 "Manual"(手册)。Linux 中的每一条命令都有一份内建手册页,说明它的作用以及所有可用选项。

```bash
ahegazy0@kali:~$ man ls
```

打开 `ls` 的完整手册。

手册内操作:
- 用 `j` 或向下方向键向下滚动
- 用 `k` 或向上方向键向上滚动
- 按 `/` 再输入文字进行搜索
- 按 `q` 退出

遇到从未使用过的工具时,首先运行 `man 工具名`。答案通常就在里面。

---

### whoami

显示你当前登录的用户。

```bash
ahegazy0@kali:~$ whoami
kali
```

若以管理员身份运行则会显示:

```
root
```

这一点在后面非常重要。有些命令需要 root 权限,普通用户身份下无法运行。明确当前账号能省去大量困惑。

---

### locate

按名称在整个文件系统中搜索文件。

```bash
ahegazy0@kali:~$ locate nmap
```

瞬间找到系统中所有名称中含有 "nmap" 的文件。

有一点需要了解:`locate` 基于一个预先生成的数据库工作,而非实时扫描磁盘。如果你刚刚安装了某软件而 `locate` 找不到,请先更新数据库:

```bash
ahegazy0@kali:~$ updatedb
```

该命令需要 root 权限。执行一次后 `locate` 即为最新。

> **注意:** `locate` 不会在你的 `/root/` 目录中查找,除非你以 root 用户身份运行。如果你以普通用户身份运行,它会假装那些文件不存在。

---

## 不可忘记的铁律

**Linux 区分大小写。** 这几乎是每个初学者都会踩的坑。

```bash
ahegazy0@kali:~$ cd Desktop     可用
ahegazy0@kali:~$ cd desktop     错误 - 文件不存在
ahegazy0@kali:~$ cd DESKTOP     错误 - 文件不存在
```

在 Linux 看来,`Desktop`、`desktop` 与 `DESKTOP` 是三个完全不同的东西。务必留意大小写。

---

## 导航操作的实际效果

```
/
└── home/
    └── kali/
        ├── Desktop/
        └── Downloads/

当前位于 /home/kali,想跳到 Desktop:
  cd Desktop             向下走一步
  cd ..                  回到 /home/kali
  cd ../..               回到 /home
  cd /                   从任何位置直接跳到根
  cd ~                   从任何位置直接跳到 /home/kali
```

---

## 命令速查

| 命令 | 作用 | 示例 |
|---|---|---|
| `pwd` | 显示当前位置 | `pwd` |
| `ls` | 列出当前目录的文件 | `ls` |
| `ls -la` | 列出包括隐藏文件在内的全部内容 | `ls -la` |
| `cd /path` | 跳转到指定目录 | `cd /etc` |
| `cd ..` | 跳到上一级 | `cd ..` |
| `cd ~` | 跳到主文件夹 | `cd ~` |
| `man cmd` | 打开命令手册 | `man ls` |
| `whoami` | 显示当前用户 | `whoami` |
| `locate file` | 在系统中查找文件 | `locate nmap` |
| `updatedb` | 更新 locate 数据库 | `updatedb` |

---

## 练习

- [ ] 打开终端,运行 `pwd` —— 看看从哪儿开始
- [ ] 运行 `cd /` 然后 `ls` —— 你已位于根目录,看看里面有什么
- [ ] 运行 `cd ~` 然后 `ls -la` —— 找到那些以 `.` 开头的隐藏文件
- [ ] 运行 `man nmap`,滚动浏览后按 `q` 退出

当前的目标不是把这些全部背下来,而是让你的手开始动起来,在终端中找到感觉。命令用得越多,自然就越熟练。

> 提示:*如需更深入的练习,建议同步完成官方 **Linux Basics for Hackers** 一书中各章末尾的练习题。*
---

[下一章:模块 2 - 文本处理](Module_02_Text_Manipulation.zh_CN.md)